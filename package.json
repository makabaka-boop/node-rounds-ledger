{
  "name": "node-rounds-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "设备巡检轮次后端服务（Node.js + SQLite）",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.1.0"
  }
}
